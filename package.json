{
  "name": "touchdesigner-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "通过 MCP 控制 TouchDesigner 的本地桥接服务",
  "bin": {
    "touchdesigner-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "test": "npm run build:test && node --test dist-test/test/*.test.js",
    "build:test": "tsc -p tsconfig.test.json"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.29.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.20.0",
    "typescript": "^5.8.0"
  },
  "engines": {
    "node": ">=20"
  }
}
